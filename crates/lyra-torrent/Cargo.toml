[package]
name = "lyra-torrent"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
lyra-core = { path = "../lyra-core" }
lyra-fs = { path = "../lyra-fs" }
thiserror.workspace = true
tokio.workspace = true
tracing.workspace = true
serde.workspace = true
serde_json.workspace = true
reqwest = "0.12" # tracker-list refresh (ngosang/trackerslist); Url re-export feeds SessionOptions.trackers
librqbit = "9" # pure-Rust BitTorrent: DHT, magnets, per-file select, streaming

[dev-dependencies]
lyra-formats = { path = "../lyra-formats" }
lyra-engine = { path = "../lyra-engine" }
librqbit = "9" # create_torrent for the local seed↔leech e2e

[lints]
workspace = true
