//! `lyra` CLI: thin socket client. Human output by default, `--json` for agents.

/// Temporary entry point; full command model lands in the next commit.
pub fn run(_args: Vec<String>) -> i32 {
    eprintln!("lyra: not yet implemented");
    4
}
